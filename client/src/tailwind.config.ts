/* ... other styles ... */

.some-component { /*  Example selector - needs to be adjusted based on the original code */
  borderRadius: {
        lg: "calc(var(--radius) * 0.8)",
        md: "calc(var(--radius) * 0.6)",
        sm: "calc(var(--radius) * 0.4)",
      },
}

/* ... rest of the stylesheet ... */